---
title: "V0.1b Real-Run Preflight Hardening Dev Log"
status: active
created_at: 2026-05-29
last_validated_at: 2026-05-29T01:36:11+0800
---
# V0.1b Real-Run Preflight Hardening Dev Log

## Scope

Approved by the user on 2026-05-29: implement `agent-run-supervisor` V0.1b real-run preflight hardening, limited to `doctor` Node/acpx probing, `allowed_roots` cwd gate, and explicit real-run refusal.

## Role split

- Claude Code: main implementation/debugging worker.
- Codex CLI: primary reviewer.
- Hermes: scope control, verification, evidence, and arbitration.

## Starting evidence

- Base branch: `origin/main` at `c40815c7f214b0f39d7071e4b86c1957bbbf6439`.
- Worktree branch: `ai/v0.1b-real-run-preflight-hardening-2026-05-28`.
- Plan: `docs/plans/2026-05-29-v0.1b-real-run-preflight-hardening.md`.
- Current status before work: Phase -1 and V0.1a complete; V0.1b allowed only with explicit user approval.

## Explicit non-approvals

No real AGENT execution, no acpx real-run launch, no persistent sessions, no Sachima integration, no real AGENT automatic replies, no public ingress, no real delivery, no Gateway lifecycle operation, no production config write, no live/default-on behavior, no worker auto-routing, no participant persistence/management UI, no `@all`, no agent-to-agent automatic routing, and no claim that `allowed_roots` is an OS/filesystem sandbox.

## Execution log

### Preflight

Read and applied:

- `GOAL.md`
- `docs/roadmap/current-status.md`
- `docs/AI_FLOW.md`
- `docs/plans/2026-05-28-v0.1a-exec-only-vertical-slice.md`
- `docs/dev_log/2026-05-28-v0.1a-exec-only-vertical-slice.md`

Preflight conclusion: the request exactly matches the dashboard's next allowed V0.1b candidate and the user's explicit approval. No roadmap contradiction found.

### RED / GREEN evidence

Initial focused RED after Claude Code wrote tests:

```text
python3 -m pytest tests/test_preflight.py tests/test_workspace_gate.py tests/test_cli_commands.py -q
ERROR tests/test_preflight.py: ModuleNotFoundError: No module named 'agent_run_supervisor.preflight'
ERROR tests/test_workspace_gate.py: ModuleNotFoundError: No module named 'agent_run_supervisor.workspace'
```

Additional RED for role-specific acpx binary probing:

```text
python3 -m pytest tests/test_cli_commands.py::test_doctor_uses_role_acpx_binary_for_version_probe -q
FAILED: payload["acpx_probe"]["binary"] == "acpx" instead of the role-provided fake-acpx path
```

Focused GREEN after implementation and Hermes' small refusal-payload/role-binary patch:

```text
python3 -m pytest tests/test_cli_commands.py::test_doctor_uses_role_acpx_binary_for_version_probe -q
. [100%]
python3 -m pytest tests/test_preflight.py tests/test_workspace_gate.py tests/test_cli_commands.py -q
................................ [100%]
```

### Claude Code main worker evidence

Claude Code was launched as the main implementation/debugging worker with the context pack in `docs/plans/2026-05-29-v0.1b-claude-main-context.md`.

- First Claude run wrote the RED tests (`tests/test_preflight.py`, `tests/test_workspace_gate.py`, and tightened `tests/test_cli_commands.py`) but hit `error_max_turns` after Bash permission denials.
- Second Claude run implemented most of the V0.1b code (`preflight.py`, `workspace.py`, runner integration, doctor probes) and hit `error_max_turns` after modifying the worktree.
- Hermes verified the worktree directly, then applied two small scope-preserving patches:
  - stable JSON refusal payload for `run` without `--no-real-run`;
  - role-specific `runner.acpx_binary` in `doctor`'s acpx version probe.

### Hermes verification evidence

```text
python3 scripts/validate_contract_fixtures.py fixtures/acpx-0.10.0
OK: fixtures/acpx-0.10.0

python3 -m pytest -q
........................................................................ [ 59%]
..................................................                       [100%]

python3 -m pytest --collect-only | tail -5
122 tests collected in 0.06s

python3 -m compileall -q src scripts tests
OK

PYTHONPATH=src python3 -m agent_run_supervisor doctor
ok=true, launched_real_agent=false, node_probe.version=24.14.0, node_probe.ok=true, acpx_probe.available=false, acpx_probe.error_detail="binary not found on PATH: acpx"

PYTHONPATH=src python3 -m agent_run_supervisor replay fixtures/acpx-0.10.0/success-codex-sentinel/stdout.ndjson
final_message=CODEX_ACPX_OK, protocol_error=false

python tools/build_docs_index.py --check
OK: 15 docs in sync with INDEX.md

python tools/docs_drift_signal.py --check
OK: drift report up to date

git diff --check
OK

changed-line scans
secret_scan=0
static_scan=0
```

### Codex primary review

Initial Codex read-only sandbox review could not inspect files because the sandbox failed before shell startup:

```text
VERDICT: BLOCK
BLOCKER: REVIEW_ENVIRONMENT bwrap loopback failure; no files inspected
```

Codex was rerun with `danger-full-access -a never` for review only, constrained by prompt to inspect and not modify files. It returned:

```text
VERDICT: BLOCK
SCORE: 88
BLOCKER: src/agent_run_supervisor/cli.py help text still said run could optionally launch acpx / no-real-run was "without launching acpx", creating a user-facing boundary overclaim.
```

Fix applied with RED/GREEN:

```text
python3 -m pytest tests/test_cli_smoke.py::test_run_help_states_real_run_refusal_boundary -q
FAILED: expected "refuses real agent launch" in run help

# after CLI help/description patch
python3 -m pytest tests/test_cli_smoke.py::test_run_help_states_real_run_refusal_boundary -q
. [100%]
python3 -m pytest tests/test_cli_smoke.py tests/test_cli_commands.py tests/test_preflight.py tests/test_workspace_gate.py -q
...................................... [100%]
```

Blocker-only re-review then passed, with one non-blocking note that `--cwd` help still said "acpx run". Hermes adopted that wording fix with RED/GREEN:

```text
# tightened test also forbids "acpx run" in run help
python3 -m pytest tests/test_cli_smoke.py::test_run_help_states_real_run_refusal_boundary -q
FAILED: "acpx run" is contained here: "Override cwd for the acpx run."

# after --cwd help patch
python3 -m pytest tests/test_cli_smoke.py::test_run_help_states_real_run_refusal_boundary -q
. [100%]
python3 -m pytest tests/test_cli_smoke.py -q
...... [100%]
```

Final blocker-only re-review passed:

```text
VERDICT: PASS
SCORE: 100
BLOCKERS: None
NOTES: CLI help now states real-agent refusal, dry-run compilation, and required --no-real-run.
```

### Claude Code auxiliary review

Claude Code auxiliary packet review result:

```text
VERDICT: PASS
SCORE: 92
BLOCKERS: None within approved scope.
```

Non-blocking notes:

- `probe_node` reports `binary="node"` even if a future phase adds a node binary override; no current role-level node override exists.
- `discover_node_binary` / `discover_acpx_binary` are currently unused helpers.
- `compile_command` now receives the resolved effective cwd; this is intentional for the cwd gate and covered by tests.

### Final boundary confirmation

Final local gates after review fixes and evidence append:

```text
python tools/build_docs_index.py --write
wrote docs/INDEX.md (15 docs)
python tools/docs_drift_signal.py --write
wrote docs/lessons/_drift_report.md
python3 scripts/validate_contract_fixtures.py fixtures/acpx-0.10.0
OK: fixtures/acpx-0.10.0
python3 -m pytest -q
........................................................................ [ 58%]
...................................................                      [100%]
python3 -m pytest --collect-only | tail -5
123 tests collected in 0.05s
python3 -m compileall -q src scripts tests
OK
PYTHONPATH=src python3 -m agent_run_supervisor doctor
ok=true, launched_real_agent=false, node_probe.version=24.14.0, node_probe.ok=true, acpx_probe.available=false, acpx_probe.error_detail="binary not found on PATH: acpx"
PYTHONPATH=src python3 -m agent_run_supervisor replay fixtures/acpx-0.10.0/success-codex-sentinel/stdout.ndjson
final_message=CODEX_ACPX_OK, protocol_error=false
python tools/build_docs_index.py --check
OK: 15 docs in sync with INDEX.md
python tools/docs_drift_signal.py --check
OK: drift report up to date
git diff --check
OK
changed-line scans
secret_scan=0
static_scan=0
```

V0.1b implements preflight hardening only. `doctor` runs version probes and fixture replay but does not launch agents. `run` without `--no-real-run` refuses before prompt reading or artifact creation. `allowed_roots` validates cwd/config only and remains explicitly not an OS/filesystem sandbox.
