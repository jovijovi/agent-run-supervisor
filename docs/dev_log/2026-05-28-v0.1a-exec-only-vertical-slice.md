# V0.1a Exec-Only Vertical Slice Dev Log

## Scope

Approved on 2026-05-28T11:23:15.732005+00:00: start the next stage after Phase -1. Interpreted as `agent-run-supervisor` V0.1a exec-only vertical slice, using the independent repo and preserving Phase -1 boundaries.

## Role split

- Hermes: controller, scope guardian, verifier, final arbiter.
- Claude Code: main implementation/debugging worker.
- Codex CLI: primary reviewer and blocker-only re-review.

## Explicit non-approvals

No persistent sessions, no Sachima behavior integration, no real AGENT automatic replies, no public ingress, no real delivery, no Gateway lifecycle operation, no production config write, no live/default-on behavior, no `@all`, and no claim that `allowed_roots` is an OS/filesystem sandbox.

## Starting evidence

- Base branch: `phase-minus-1-acpx-contract-spike` at `9869755`.
- Worktree branch: `v0.1a-exec-only-vertical-slice`.
- Phase -1 fixtures: `fixtures/acpx-0.10.0/`.
- Phase -1 reviews: Codex PASS 94/100, Claude Code PASS 94/100.

## Implementation evidence

### Claude Code main worker

Claude Code was launched as the main implementation/debugging worker with the context pack in `docs/plans/2026-05-28-v0.1a-claude-main-context.md`.

- First run produced the package skeleton, core modules, and test suite, but timed out before final CLI command hookup.
- Hermes verification after that run produced meaningful RED failures: `python3 -m pytest -q` failed only on unimplemented CLI command functions and `command.argv.json` shape.
- Two focused Claude continuation attempts also hit `error_max_turns` without changing those remaining files.
- Hermes applied the final small CLI hookup patch to unblock verification, preserving the Claude-authored module/test structure.

### RED/GREEN evidence

Initial focused RED after Claude main worker partial implementation:

```text
python3 -m pytest -q
FAILED tests/test_cli_commands.py::* because commands.py raised NotImplementedError
FAILED tests/test_runner_dry_run.py::test_dry_run_writes_run_dir_with_safe_artifacts because command.argv.json was {"argv": [...]} instead of an argv array
```

Targeted GREEN after CLI hookup and artifact-shape fix:

```text
python3 -m pytest tests/test_cli_commands.py tests/test_runner_dry_run.py -q
13 passed
```

Full GREEN:

```text
python3 scripts/validate_contract_fixtures.py fixtures/acpx-0.10.0
OK: fixtures/acpx-0.10.0
python3 -m pytest -q
98 passed
python3 -m compileall -q src scripts tests
OK
secret_scan=0
static_scan=0
git diff --check
OK
PYTHONPATH=src python3 -m agent_run_supervisor doctor
ok=true, launched_real_agent=false
PYTHONPATH=src python3 -m agent_run_supervisor replay fixtures/acpx-0.10.0/success-codex-sentinel/stdout.ndjson
final_message=CODEX_ACPX_OK, protocol_error=false
```

### Implemented files

- `src/agent_run_supervisor/role.py` — AgentRoleSpec validation and hashing.
- `src/agent_run_supervisor/policy.py` — role-bound acpx policy/argv compiler.
- `src/agent_run_supervisor/exit_classifier.py` — acpx exit-code/status classifier.
- `src/agent_run_supervisor/parser.py` — Phase -1 observed JSON-RPC stdout parser/replay.
- `src/agent_run_supervisor/event_store.py` — secure run artifact store with 0700/0600 permissions.
- `src/agent_run_supervisor/redaction.py` — prompt/env/argv/result redaction helpers.
- `src/agent_run_supervisor/runner.py` — dry-run artifact path and final-outcome packaging.
- `src/agent_run_supervisor/cli.py` / `commands.py` — `validate-role`, `replay`, `doctor`, and safe `run --no-real-run` CLI.
- `tests/` — fixture-driven and unit coverage for the V0.1a slice.

### Boundary confirmation

No persistent sessions, no Sachima integration, no real AGENT automatic replies, no public ingress, no real delivery, no Gateway lifecycle operation, no production config write, and no claim that `allowed_roots` is a security sandbox.

### Codex primary review

Codex CLI read-only review result:

```text
VERDICT: PASS
SCORE: 92/100
BLOCKERS: None
```

Non-blocking notes carried forward:

- `doctor` reports the Node requirement rather than probing actual Node/acpx versions.
- Future real-run work should add explicit cwd-vs-allowed-roots validation before enabling live execution.
