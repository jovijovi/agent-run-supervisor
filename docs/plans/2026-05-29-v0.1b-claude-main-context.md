---
title: "V0.1b Claude Code Main Worker Context"
status: active
created_at: 2026-05-29
last_validated_at: 2026-05-29T01:36:11+0800
---
# Claude Code Main Worker Context — V0.1b Real-Run Preflight Hardening

You are the main implementation/debugging worker for `agent-run-supervisor` V0.1b.

## Repo / worktree

- Worktree: `/home/ecs-user/workspace/hermes/worktrees/agent-run-supervisor/v0.1b-real-run-preflight-hardening-2026-05-28`
- Branch: `ai/v0.1b-real-run-preflight-hardening-2026-05-28`
- Base: `origin/main` at `c40815c`

## Required reading

Read before editing:

1. `AGENTS.md`
2. `GOAL.md`
3. `docs/AI_FLOW.md`
4. `docs/roadmap/current-status.md`
5. `docs/plans/2026-05-29-v0.1b-real-run-preflight-hardening.md`
6. `docs/dev_log/2026-05-29-v0.1b-real-run-preflight-hardening.md`
7. Relevant implementation/tests:
   - `src/agent_run_supervisor/commands.py`
   - `src/agent_run_supervisor/runner.py`
   - `src/agent_run_supervisor/role.py`
   - `src/agent_run_supervisor/policy.py`
   - `tests/test_cli_commands.py`
   - `tests/test_runner_dry_run.py`
   - `tests/conftest.py`

## Approved scope

Implement only:

1. `doctor` Node/acpx version probes.
2. `allowed_roots` cwd/config validation gate.
3. Explicit real-run refusal gate.
4. Required tests and docs/dev-log evidence.

## Hard non-approvals

Do not implement or imply:

- real AGENT execution;
- subprocess launch of acpx for `run`;
- persistent sessions;
- Sachima integration;
- automatic AGENT replies;
- public ingress / real delivery;
- Gateway lifecycle operations;
- production config writes;
- live/default-on behavior;
- worker auto-routing;
- participant management UI;
- `@all`;
- agent-to-agent automatic routing;
- `allowed_roots` as an OS/filesystem sandbox.

## TDD requirement

Use strict TDD:

1. Write focused failing tests first.
2. Run focused tests and confirm meaningful RED failures.
3. Implement minimal code.
4. Run focused tests and full `python3 -m pytest -q`.
5. Do not count harness errors as RED.

## Implementation guidance

- Prefer stdlib-only helpers.
- A new `src/agent_run_supervisor/preflight.py` or `workspace.py` is fine if it keeps `commands.py` small.
- `doctor` may run version-only commands such as `node --version` and `acpx --version` through a narrow helper. It must not run `acpx exec` or launch an agent.
- `acpx` may be unavailable in CI; represent that safely in `acpx_probe` unless a role points at an explicit unavailable binary and the plan/tests require failure.
- Cwd validation should use resolved paths and pass when effective cwd is equal to or inside any allowed root.
- Fail outside allowed roots before creating run artifacts.
- Real-run refusal should happen before reading prompt/artifact creation if feasible, and should never create runs.
- Keep `allowed_roots_security_boundary=false` and the disclaimer in metadata/results/docs.

## Expected verification

Run at least:

```bash
python3 -m pytest tests/test_cli_commands.py tests/test_runner_dry_run.py -q
python3 -m pytest -q
python3 -m compileall -q src scripts tests
```

Do not commit. Leave final commit/PR to Hermes after independent verification and review.
